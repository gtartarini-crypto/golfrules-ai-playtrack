import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'clublink.golfrules.playtrack',
  appName: 'GolfRules & PlayTrack',
  webDir: 'dist'
};

export default config;
